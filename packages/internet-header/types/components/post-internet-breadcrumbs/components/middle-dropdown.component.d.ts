import { IBreadcrumbItem } from '../../../models/breadcrumbs.model';
export declare const MiddleDropdown: (props: {
  items: IBreadcrumbItem[];
  dropdownOpen: boolean;
  clickHandler: (event?: MouseEvent) => void;
  focusable?: boolean | undefined;
}) => any;
