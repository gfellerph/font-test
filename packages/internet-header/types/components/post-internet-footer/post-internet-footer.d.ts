export declare class PostInternetFooter {
  liveSupportEnabled: boolean;
  constructor();
  /**
   * Check if the unblu live support script is loaded
   * @returns Boolean
   */
  private unbluEnabled;
  render(): any;
}
